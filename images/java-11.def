IMAGE_COMPATIBLE(sle-15.2)
IMAGE_TITLE([Java 11 development container])
IMAGE_DESCRIPTION([Environment for Java 11 development])
IMAGE_TAG(java-devel)
IMAGE_VERSION(11)

BUILD_INSTRUCTIONS([
# Work around https://github.com/openSUSE/obs-build/issues/487
RUN zypper install -y openSUSE-release-appliance-docker

# Development packages
RUN zypper install -y --no-recommends osc java-11-openjdk-devel maven git-core
])
